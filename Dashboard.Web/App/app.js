(function () {
    var app = angular.modeule("dashboard", ["ngRoute"]);

    app.config(function ($routeProvider) {
        $routeProvider
            .when()
            .when()
            .otherwise()
    });



}());